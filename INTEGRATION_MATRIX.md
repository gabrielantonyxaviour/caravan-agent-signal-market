# Integration Matrix

Hackathon: Agora Agents Hackathon
Idea: CARAVAN Agent-to-Agent Signal Market
Updated: 2026-05-22 07:25 IST

Final product status: `auth-blocked`

| Dependency | Real access path | Env / credential | Proof command or browser proof | Fixture policy | Status | Blocker |
|---|---|---|---|---|---|---|
| CoinGecko public market API | Server-side fetch through same-origin route `/api/market`; Node replay fetch | No credential | `curl -s http://127.0.0.1:3037/api/market` returned `ok=true`, 3 symbols; `npm run replay` returned `marketStatus: "live"`; E2E/visual QA reached `Live market frame` | If unavailable, app falls back to `fixtureMarket` and labels `DEMO FIXTURE` | proven | Public API availability/rate limits. |
| Browser wallet provider | EIP-1193 `window.ethereum` | User wallet extension/profile | `npm run e2e:readiness` injected-provider app-side proof: account request, chain switch to `0x4cef52`, `personal_sign`, signed session screenshot | No wallet provider shows blocked state; no fake account | implemented; real-wallet proof blocked | No unlocked real wallet extension in local headless proof. |
| Canteen ARC CLI | `/Users/gabrielantonyxaviour/.local/bin/arc-canteen` | GitHub device auth / Canteen token | `arc-canteen login` generated code `6916-3154`; `agent-browser --profile "Default"` reached GitHub authorize page; `Authorize the-canteen-dev` disabled; screenshot `outputs/readiness-arc-login-disabled.png`; status/rpc still not logged in | No CLI/RPC success faked | auth-blocked | GitHub OAuth authorization button disabled. |
| Arc Testnet RPC | `arc-canteen rpc eth_chainId`; optional `ARC_TESTNET_RPC_URL` for replay submit | `ARC_TESTNET_RPC_URL` | `arc-canteen rpc eth_chainId` returns not logged in; blocked submit command returns missing env | UI/report says no Arc tx configured until RPC exists | blocked | No RPC URL/token. |
| Arc transaction submission | `scripts/replay-demo.ts -- --submit` via `viem` wallet client | `ARC_TESTNET_RPC_URL`; `PRIVATE_KEY`; funded Arc Testnet wallet | Submit path is coded and blocks without env; no send attempted without credentials | No tx hash or Arcscan URL displayed unless real tx hash exists | blocked | Missing RPC URL, private key, and funded testnet wallet. |
| Arcscan explorer | `https://testnet.arcscan.app/tx/<txHash>` | Real tx hash | URL generated only when `txHash` exists; current UI shows tx proof blocked | No placeholder explorer link allowed | blocked | No tx hash. |
| Circle/USDC | Arc Testnet USDC address in `ARC_TESTNET.usdc` | No Circle dashboard key in app; faucet/wallet needed for real payment | UI shows USDC contract address and `0.12 USDC` ticket; no transfer claimed | USDC ticket is fixture/payment-ready unless real Arc tx exists | label-proven only | Real USDC transfer requires Arc wallet funding and submit proof. |
| Solidity registry contract | Local Solidity source | No deploy key | `solc@0.8.30 --bin --abi` wrote ABI/bin under `/tmp/caravan-readiness-contract-check` | Contract described as intended registry, not deployed infrastructure | compile-proven | No deployment address or contract call proof. |
| Vercel production deployment | Vercel project `rax-tech/caravan-agent-signal-market` | Existing Vercel CLI auth | Prior hardening report proved production alias; this readiness pass did not redeploy after local auth/API changes | Public app must show fixture/payment labels when no tx configured | existing demo proven; updated deploy not claimed | Redeploy needed after auth/API changes if public testing is required. |
| GitHub public repo | `gh` and `origin` remote | Existing GitHub CLI auth | `gh repo view ...` returned public repo | Repo docs must not claim real Arc tx | proven | Current readiness changes are local unless pushed later. |
| Formal M2 `/polish` route | `PLAYWRIGHT_CLI_REMOTE=m2worker`; `playwright-cli-sessions` | SSH access to `m2worker` | `npx playwright-cli-sessions@latest browser start` failed SSH preflight to `100.115.214.82:22`; report saved under `.playwright-sessions/.reports/2026-05-22T01-42-52-047-...md` | Do not label as formal polish pass | blocked | M2 worker unreachable over SSH. |
| Local visual QA fallback | Local headless Chrome via `scripts/local-visual-qa.mjs` | No external credential | `CARAVAN_QA_URL=http://localhost:3037 npm run visual:qa` passed 375/768/1440 screenshots and primary live-check flow | Label as `local-visual-qa-passed; formal-polish-blocked-by-m2` | proven fallback | Not a formal `/polish` score. |
| Google Form submission portal | Google Form URL recorded in prior builder context | Gabriel profile/session; final approval | Not touched in readiness pass | No submission claim until explicitly approved and submitted | blocked | Demo video URL missing; legal/final submit requires Gabriel approval. |
