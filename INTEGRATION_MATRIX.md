# Integration Matrix

Hackathon: Agora Agents Hackathon
Idea: CARAVAN Agent-to-Agent Signal Market
Updated: 2026-05-22 05:26 IST

Final product status: demo-ready

| Dependency | Real access path | Env / credential | Proof command or browser proof | Fixture policy | Status | Blocker |
|---|---|---|---|---|---|---|
| CoinGecko public market API | Browser/client fetch and Node replay fetch to `https://api.coingecko.com/api/v3/simple/price` | No credential | `curl` returned BTC/ETH/SOL price flags; `npm run replay` returned `marketStatus: "live"`; `npm run visual:qa` reached `Live market frame` | If unavailable, app falls back to `fixtureMarket` and labels `DEMO FIXTURE` | proven | Public API availability and rate limits. |
| Canteen ARC CLI | `/Users/gabrielantonyxaviour/.local/bin/arc-canteen` | Requires Canteen/GitHub login token | `arc-canteen status` and `arc-canteen rpc eth_chainId` both returned `Not logged in. Run arc-canteen login first.` | No CLI/RPC success is faked | blocked | GitHub device authorization was previously blocked by disabled authorize button; current CLI has no login. |
| Arc Testnet RPC | `arc-canteen rpc eth_chainId`; optional `ARC_TESTNET_RPC_URL` for replay submit | `ARC_TESTNET_RPC_URL` | `env -u ARC_TESTNET_RPC_URL -u PRIVATE_KEY npm run replay -- --submit --fixture` returned `status: "blocked"` with missing env reason | UI/report must say no Arc tx configured until RPC exists | blocked | No RPC URL/token. |
| Arc transaction submission | `scripts/replay-demo.ts -- --submit` via `viem` wallet client | `ARC_TESTNET_RPC_URL`; `PRIVATE_KEY`; funded Arc testnet wallet | Submit path is coded and blocks without env; no send attempted without credentials | No tx hash or Arcscan URL displayed unless `NEXT_PUBLIC_ARC_TX_HASH` or submit result exists | blocked | Missing RPC URL, private key, and funded testnet wallet. |
| Arcscan explorer | `https://testnet.arcscan.app/tx/<txHash>` | Requires real tx hash | URL is generated only when `txHash` exists; browser/public HTML shows no Arcscan link without tx | No placeholder explorer link allowed | blocked | No tx hash. |
| Circle/USDC | Arc Testnet USDC address in `ARC_TESTNET.usdc` | No Circle dashboard key in app; faucet/wallet needed for real payment | UI shows USDC contract address and `0.12 USDC` ticket; no transfer claimed | USDC ticket is fixture/payment-ready unless real Arc tx exists | demo-ready label only | Real USDC transfer requires Arc wallet funding and submit proof. |
| Solidity registry contract | Local Solidity source | No deploy key | `npx --yes solc@0.8.30 --bin --abi contracts/SignalMarketRegistry.sol -o /tmp/caravan-contract-check` produced ABI/bin | Contract is described as intended registry, not deployed infrastructure | compile-proven | No deployment address or contract call proof. |
| Vercel production deployment | Vercel project `rax-tech/caravan-agent-signal-market` | Existing Vercel CLI auth | `vercel deploy --prod --yes` produced `https://caravan-agent-signal-market-oq7fnku2m-rax-tech.vercel.app`; aliased to `https://caravan-agent-signal-market.vercel.app`; `curl -I` HTTP 200 | Public app must show fixture/payment labels when no tx configured | proven | None for demo; repo should be pushed if submission reviewers inspect source. |
| GitHub public repo | `gh` and `origin` remote | Existing GitHub CLI auth for `gabrielantonyxaviour` | `gh repo view gabrielantonyxaviour/caravan-agent-signal-market` returned public repo; `git ls-remote` matched `origin/main` at `6922f9b...` before local hardening edits | Repo docs must not claim real Arc tx | exists; local changes pending | Hardening edits not pushed at time of matrix update. |
| Formal M2 /polish route | `PLAYWRIGHT_CLI_REMOTE=m2worker`; `playwright-cli-sessions` | SSH access to `m2worker` | `browser start` failed SSH preflight to `100.115.214.82:22` timeout; report saved under `.playwright-sessions/.reports/2026-05-21T23-51-22-283-...md` | Do not label as formal polish pass | blocked | M2 worker unreachable over SSH. |
| Local visual QA fallback | Local headless Chrome via `scripts/local-visual-qa.mjs` | No external credential | `npm run visual:qa` captured 375/768/1440 screenshots and passed primary live-check flow | Label as `local-visual-qa-passed; formal-polish-blocked-by-m2` | proven fallback | Not a formal `/polish` score. |
| Google Form submission portal | Google Form URL recorded in builder report | Gabriel profile/session; final approval | Builder report identified fields; hardening did not prefill or final-submit | No submission claim until explicitly approved and submitted | blocked | Demo video URL missing; legal/final submit requires Gabriel approval. |
