# Feature Matrix

Hackathon: Agora Agents Hackathon
Idea: CARAVAN Agent-to-Agent Signal Market
Updated: 2026-05-22 05:26 IST

Final product status: demo-ready

| Feature / claim | Source evidence | Implementation path | Test / proof | Status | Gap / blocker |
|---|---|---|---|---|---|
| Primary demo flow: Atlas publishes a signal, Bravo prices a USDC ticket, Coda audits, Bravo refuses execution | `BUILD_PLAN.md` demo path; `EXECUTION_PACKET.md` demo script | `src/lib/caravan.ts`; `src/components/HeroPanel.tsx`; `src/components/SignalRoom.tsx`; `src/components/ProofSections.tsx` | `npm test` 4/4; `npm run replay`; `npm run visual:qa`; screenshots `outputs/screenshots/hardening-1440-fixture.png` and `outputs/screenshots/hardening-1440-live.png` | demo-ready | Real Arc payment/tx not proven; UI labels fixture/payment-ready/confirmed states. |
| Live public market data frame | `BUILD_PLAN.md` live price ingestion goal | `fetchMarketFrame()` in `src/lib/caravan.ts`; `Run live check` in `src/components/CaravanExperience.tsx` | CoinGecko curl returned BTC/ETH/SOL prices; `npm run replay` returned `marketStatus: "live"`; `npm run visual:qa` reached `Live market frame` | proven | Depends on public CoinGecko availability/rate limits; deterministic fixture fallback remains. |
| Deterministic replay fallback | `BUILD_PLAN.md` says replay is acceptance spine | `scripts/replay-demo.ts`; `fixtureMarket` in `src/lib/caravan.ts` | `npm run replay -- --fixture` returned refusal path and `paymentStatus: "fixture"`; unit test covers fixture refusal | proven | Fixture is intentionally labeled; no chain transaction is claimed for fixture mode. |
| USDC-priced signal ticket and buyer refusal | `UI_TEMPLATE_PLAN.md` first-screen judge moment | `askingPriceUsdc: 0.12`; `buildCaravanRun()` cost accounting; `SignalLedger`; `CostPanel` | Unit test asserts `0.12` ask and negative net edge; browser screenshots show fee table and `REFUSE` | proven as simulation | Real USDC transfer blocked until Arc RPC auth and funded wallet exist. |
| Arc/Circle proof/status panel | `BUILD_PLAN.md`; `SPONSOR_ACCESS_PLAN.md`; builder report sponsor/API status | `ProofSections.tsx`; `ARC_TESTNET` constants; `NEXT_PUBLIC_ARC_TX_HASH` optional path | Public Vercel HTML shows `fixture ticket; no chain transaction`; `npm run replay -- --submit --fixture` blocks on missing env; `arc-canteen status` says not logged in | demo-ready with blocked live proof | No Canteen/Arc login token, no funded testnet wallet, no Arcscan tx. |
| Solidity event registry for signal sale/refusal | `README.md` proof surfaces; `contracts/SignalMarketRegistry.sol` | `contracts/SignalMarketRegistry.sol` | `npx --yes solc@0.8.30 --bin --abi contracts/SignalMarketRegistry.sol -o /tmp/caravan-contract-check` produced ABI and bytecode | compile-proven | Contract is not deployed and not called by the app. |
| Public demo deployment | Builder report deployment claim | Vercel project `rax-tech/caravan-agent-signal-market`; alias `https://caravan-agent-signal-market.vercel.app` | `vercel deploy --prod --yes` succeeded; `vercel inspect` shows deployment `dpl_27WhLwXN6k7ENDwmdCaFiQJ63WJg` ready and aliased; `curl -I` returned HTTP 200 | proven | Deployment was made from local hardening workspace; commit/push status tracked separately. |
| Public repo | Builder report repo claim | `origin https://github.com/gabrielantonyxaviour/caravan-agent-signal-market.git` | `gh repo view` shows public repo under `gabrielantonyxaviour`; hardening commit `0795f2c` pushed to `origin/main` | proven | None for current demo source. |
| Submission packet and portal prep | `SUBMISSION_PORTAL_PLAN.md`; `EXECUTION_PACKET.md`; builder report portal status | `EXECUTION_PACKET.md`; output report docs | Portal fields were identified in builder report; hardening report records next actions | blocked for final submission | Demo video URL missing; final legal attestation/final submit require explicit Gabriel approval. |
