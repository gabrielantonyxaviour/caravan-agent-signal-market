# Feature Matrix

Hackathon: Agora Agents Hackathon
Idea: CARAVAN Agent-to-Agent Signal Market
Updated: 2026-05-22 07:25 IST

Final product status: `auth-blocked`

| Feature / claim | Source evidence | Implementation path | Test / proof | Status | Gap / blocker |
|---|---|---|---|---|---|
| Primary demo flow: Atlas publishes a signal, Bravo prices a USDC ticket, Coda audits, Bravo refuses execution | `BUILD_PLAN.md` demo path; `EXECUTION_PACKET.md` demo script | `src/lib/caravan.ts`; `src/components/HeroPanel.tsx`; `src/components/SignalRoom.tsx`; `src/components/ProofSections.tsx` | `npm test` 5/5; `npm run replay`; `npm run e2e:readiness`; `npm run visual:qa`; screenshot `outputs/screenshots/hardening-1440-live.png` | testing-ready locally | Real Arc payment/tx not proven; UI labels fixture/payment-ready/confirmed states. |
| Same-origin live market data frame | Readiness browser failure showed direct CoinGecko client fetch was brittle | `app/api/market/route.ts`; `fetchBrowserMarketFrame()` in `src/components/CaravanExperience.tsx`; `fetchMarketFrame()` in `src/lib/caravan.ts` | `curl -s http://127.0.0.1:3037/api/market` returned BTC/ETH/SOL; `npm run e2e:readiness` and `npm run visual:qa` reached `Live market frame` | proven | Public CoinGecko availability/rate limits. |
| Web3 wallet auth panel | Readiness contract requires real auth decision/path for web3 products | `src/components/WalletAuthPanel.tsx`; `src/lib/wallet-auth.ts`; nav `Wallet auth` link | `npm run e2e:readiness` passed no-wallet blocked state and injected EIP-1193 signed session on Arc chain `0x4cef52`; screenshots `outputs/readiness-e2e/no-wallet-375.png`, `outputs/readiness-e2e/wallet-connected-1440.png` | implemented; real-wallet proof blocked | Needs unlocked real wallet/profile to prove extension signature. |
| Deterministic replay fallback | `BUILD_PLAN.md` says replay is acceptance spine | `scripts/replay-demo.ts`; `fixtureMarket` in `src/lib/caravan.ts` | `npm run replay -- --fixture` returned refusal path and `paymentStatus: "fixture"`; unit test covers fixture refusal | proven | Fixture intentionally labeled; no chain transaction claimed for fixture mode. |
| USDC-priced signal ticket and buyer refusal | `UI_TEMPLATE_PLAN.md` first-screen judge moment | `askingPriceUsdc: 0.12`; `buildCaravanRun()` cost accounting; `SignalLedger`; `CostPanel` | Unit test asserts `0.12` ask and negative net edge; browser screenshots show fee table and `REFUSE` | proven as simulation | Real USDC transfer blocked until Arc RPC auth and funded wallet exist. |
| Arc/Circle proof/status panel | `BUILD_PLAN.md`; `SPONSOR_ACCESS_PLAN.md`; prior hardening report sponsor/API status | `ProofSections.tsx`; `WalletAuthPanel`; `ARC_TESTNET` constants; `NEXT_PUBLIC_ARC_TX_HASH` optional path | `arc-canteen status` / `rpc eth_chainId` return not logged in; `npm run replay -- --submit --fixture` blocks on missing env; UI says tx proof blocked | auth-blocked | GitHub OAuth authorize button disabled; no RPC token, wallet key, or tx hash. |
| Solidity event registry for signal sale/refusal | `README.md` proof surfaces; `contracts/SignalMarketRegistry.sol` | `contracts/SignalMarketRegistry.sol` | `rm -rf /tmp/caravan-readiness-contract-check && npx --yes solc@0.8.30 --bin --abi contracts/SignalMarketRegistry.sol -o /tmp/caravan-readiness-contract-check` passed | compile-proven | Contract is not deployed and not called by the app. |
| No dummy action surface | Readiness contract button/link audit | Buttons: `Run live check`, `Replay deterministic fixture`, `Connect wallet`; anchors: `#room`, `#auth`, `#proof`, `#packet` | `npm run e2e:readiness` action audit checked 3 visible buttons and found 0 invalid actions; browser nav anchors are real section targets | passed | None for visible local page. |
| Public repo | Builder/hardening report repo claim | `origin https://github.com/gabrielantonyxaviour/caravan-agent-signal-market.git` | `gh repo view gabrielantonyxaviour/caravan-agent-signal-market --json nameWithOwner,visibility,url` returned public repo | proven | Current readiness changes are local unless pushed/deployed later. |
| Public demo deployment | Prior hardening public URL | Existing Vercel project `rax-tech/caravan-agent-signal-market`; alias `https://caravan-agent-signal-market.vercel.app` | Prior hardening report proved deploy `dpl_27WhLwXN6k7ENDwmdCaFiQJ63WJg`; this readiness pass tested local updated code | existing demo only | Current wallet auth/API readiness changes not claimed deployed. |
| Submission packet and portal prep | `SUBMISSION_PORTAL_PLAN.md`; `EXECUTION_PACKET.md` | Docs and output reports | Readiness report records blockers; no final submit attempted | blocked | Demo video URL missing; final legal attestation/final submit require explicit Gabriel approval. |
